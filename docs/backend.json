
{
  "entities": {
    "User": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "User",
      "type": "object",
      "description": "Represents a user account in the UnMac application, storing profile information and roles. Assumes an external authentication system for passwords.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the User entity."
        },
        "email": {
          "type": "string",
          "description": "The user's unique email address, used for communication and login (via external authentication).",
          "format": "email"
        },
        "firstName": {
          "type": "string",
          "description": "The first name of the user."
        },
        "lastName": {
          "type": "string",
          "description": "The last name of the user."
        },
        "role": {
          "type": "string",
          "description": "The role assigned to the user within the system (e.g., 'user', 'admin')."
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp when the user account was created.",
          "format": "date-time"
        },
        "updatedAt": {
          "type": "string",
          "description": "Timestamp when the user account information was last updated.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "email",
        "role",
        "createdAt"
      ]
    },
    "Subscription": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Subscription",
      "type": "object",
      "description": "Manages a user's subscription plan, including free trials and paid memberships, determining service access duration.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Subscription entity."
        },
        "userId": {
          "type": "string",
          "description": "Reference to the User associated with this subscription. (Relationship: User 1:N Subscription)"
        },
        "planType": {
          "type": "string",
          "description": "The type of plan (e.g., 'FreeTrial', 'PaidMonthly')."
        },
        "startDate": {
          "type": "string",
          "description": "The date and time when the subscription became active.",
          "format": "date-time"
        },
        "endDate": {
          "type": "string",
          "description": "The date and time when the subscription is set to expire.",
          "format": "date-time"
        },
        "status": {
          "type": "string",
          "description": "The current status of the subscription (e.g., 'Active', 'Expired', 'Canceled')."
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp when the subscription record was created.",
          "format": "date-time"
        },
        "updatedAt": {
          "type": "string",
          "description": "Timestamp when the subscription record was last updated.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "userId",
        "planType",
        "startDate",
        "endDate",
        "status",
        "createdAt"
      ]
    },
    "UnblockRequest": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "UnblockRequest",
      "type": "object",
      "description": "Stores details for a user's request to unblock a specific MAC address on a chosen WiFi network.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the UnblockRequest entity."
        },
        "userId": {
          "type": "string",
          "description": "Reference to the User who made this unblock request. (Relationship: User 1:N UnblockRequest)"
        },
        "subscriptionId": {
          "type": "string",
          "description": "Reference to the Subscription under which this unblock request was made. (Relationship: Subscription 1:N UnblockRequest)"
        },
        "macAddress": {
          "type": "string",
          "description": "The MAC address requested to be unblocked (e.g., '00:1A:2B:3C:4D:5E')."
        },
        "deviceName": {
          "type": "string",
          "description": "The name of the device associated with the MAC address (e.g., 'My Laptop')."
        },
        "wifiProvider": {
          "type": "string",
          "description": "The WiFi service provider (e.g., 'Jio', 'Airtel', 'BSNL')."
        },
        "wifiName": {
          "type": "string",
          "description": "The SSID (name) of the WiFi network (e.g., 'HomeWiFi')."
        },
        "status": {
          "type": "string",
          "description": "The current status of the unblock request (e.g., 'Processing', 'Unblocked', 'Failed', 'Expired')."
        },
        "requestDate": {
          "type": "string",
          "description": "Timestamp when the unblock request was submitted.",
          "format": "date-time"
        },
        "unblockedAt": {
          "type": "string",
          "description": "Optional timestamp when the MAC address was successfully unblocked.",
          "format": "date-time"
        },
        "expirationDate": {
          "type": "string",
          "description": "The date and time when the unblock service for this request is set to expire, based on the user's plan.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "userId",
        "subscriptionId",
        "macAddress",
        "wifiProvider",
        "wifiName",
        "status",
        "requestDate",
        "expirationDate"
      ]
    },
    "Payment": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Payment",
      "type": "object",
      "description": "Records details of payment transactions made by users, primarily for subscription plans, including UPI specifics.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Payment entity."
        },
        "subscriptionId": {
          "type": "string",
          "description": "Reference to the Subscription this payment is for. (Relationship: Subscription 1:N Payment)"
        },
        "userId": {
          "type": "string",
          "description": "Reference to the User who made this payment. (Relationship: User 1:N Payment)"
        },
        "amount": {
          "type": "number",
          "description": "The monetary amount of the payment."
        },
        "currency": {
          "type": "string",
          "description": "The currency of the payment (e.g., 'INR')."
        },
        "paymentDate": {
          "type": "string",
          "description": "The date and time the payment was made.",
          "format": "date-time"
        },
        "status": {
          "type": "string",
          "description": "The status of the payment (e.g., 'Pending', 'Completed', 'Failed', 'Refunded')."
        },
        "paymentMethod": {
          "type": "string",
          "description": "The method used for payment (e.g., 'UPI')."
        },
        "transactionId": {
          "type": "string",
          "description": "The unique transaction ID provided by the payment system, required for UPI validation."
        },
        "upiApp": {
          "type": "string",
          "description": "The UPI application used for payment (e.g., 'PhonePe')."
        },
        "providerReferenceId": {
          "type": "string",
          "description": "An optional reference ID from the payment gateway provider for external tracking."
        }
      },
      "required": [
        "id",
        "subscriptionId",
        "userId",
        "amount",
        "currency",
        "paymentDate",
        "status",
        "paymentMethod",
        "transactionId",
        "upiApp"
      ]
    },
    "ContactInquiry": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "ContactInquiry",
      "type": "object",
      "description": "Stores messages submitted by users through the website's contact form, facilitating communication and support.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the ContactInquiry entity."
        },
        "name": {
          "type": "string",
          "description": "The name of the person submitting the inquiry."
        },
        "email": {
          "type": "string",
          "description": "The email address for responding to the inquiry.",
          "format": "email"
        },
        "subject": {
          "type": "string",
          "description": "The subject line of the inquiry message."
        },
        "message": {
          "type": "string",
          "description": "The full content of the inquiry message."
        },
        "submissionDate": {
          "type": "string",
          "description": "Timestamp when the inquiry was submitted.",
          "format": "date-time"
        },
        "status": {
          "type": "string",
          "description": "The processing status of the inquiry (e.g., 'New', 'InProgress', 'Resolved')."
        }
      },
      "required": [
        "id",
        "name",
        "email",
        "subject",
        "message",
        "submissionDate",
        "status"
      ]
    },
    "RedeemCode": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "RedeemCode",
      "type": "object",
      "description": "Stores redeemable voucher codes for premium access activation.",
      "properties": {
        "id": {
          "type": "string",
          "description": "The actual redeem code (e.g., 'PROMO100')."
        },
        "planType": {
          "type": "string",
          "description": "The plan type this code activates (e.g., 'PaidMonthly')."
        },
        "isUsed": {
          "type": "boolean",
          "description": "Whether the code has been redeemed (for single-use codes)."
        },
        "multiUse": {
          "type": "boolean",
          "description": "Whether the code can be used multiple times."
        },
        "createdAt": {
          "type": "string",
          "format": "date-time"
        }
      },
      "required": ["id", "planType", "isUsed", "multiUse", "createdAt"]
    }
  },
  "auth": {
    "providers": [
      "password",
      "anonymous",
      "google.com"
    ]
  },
  "firestore": {
    "structure": [
      {
        "path": "/users/{userId}",
        "definition": {
          "entityName": "User",
          "schema": {
            "$ref": "#/backend/entities/User"
          },
          "description": "Stores individual user profiles."
        }
      },
      {
        "path": "/roles_admin/{adminId}",
        "definition": {
          "entityName": "User",
          "schema": {
            "$ref": "#/backend/entities/User"
          },
          "description": "Admin privileges marker."
        }
      },
      {
        "path": "/users/{userId}/subscriptions/{subscriptionId}",
        "definition": {
          "entityName": "Subscription",
          "schema": {
            "$ref": "#/backend/entities/Subscription"
          },
          "description": "User subscriptions."
        }
      },
      {
        "path": "/users/{userId}/unblockRequests/{unblockRequestId}",
        "definition": {
          "entityName": "UnblockRequest",
          "schema": {
            "$ref": "#/backend/entities/UnblockRequest"
          },
          "description": "User unblock requests."
        }
      },
      {
        "path": "/users/{userId}/payments/{paymentId}",
        "definition": {
          "entityName": "Payment",
          "schema": {
            "$ref": "#/backend/entities/Payment"
          },
          "description": "User payments."
        }
      },
      {
        "path": "/contactInquiries/{inquiryId}",
        "definition": {
          "entityName": "ContactInquiry",
          "schema": {
            "$ref": "#/backend/entities/ContactInquiry"
          },
          "description": "Public inquiries."
        }
      },
      {
        "path": "/redeemCodes/{codeId}",
        "definition": {
          "entityName": "RedeemCode",
          "schema": {
            "$ref": "#/backend/entities/RedeemCode"
          },
          "description": "Storage for redeemable vouchers."
        }
      }
    ]
  }
}
