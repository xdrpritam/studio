[build]
  command = "npm run build"
  publish = ".next"

[functions]
  # Increase timeout for Genkit AI operations and other server-side tasks
  timeout = 60

[[plugins]]
  package = "@netlify/plugin-nextjs"
